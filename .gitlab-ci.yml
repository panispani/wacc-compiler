before_script:
  - sbt sbt-version

build:
  script:
    - make

test:
  script:
    # Execute your project's tests
    - make # Need to make in order to generate antlr files
    - sbt clean test
    - ./run_all_tests.py
